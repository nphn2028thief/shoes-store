import appSlice from "./app/app_slice";
import authSlice from "./auth/auth_slice";
import categorySlice from "./category/cagetory_slice";

const reducer = {
  appSlice,
  authSlice,
  categorySlice
};

export default reducer;
