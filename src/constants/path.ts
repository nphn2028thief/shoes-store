enum EPath {
  HOME = "/",
  ABOUT = "/about",
  CATEGORY = "/category",
  CATEGORY_SLUG = "/category/:slug",
  CONTACT = "/contact",
  SIGNIN = "/signin",
  PROFILE = "/my-profile",
  TEST = "/test",
  NOTFOUND = "*",
}

export default EPath;
